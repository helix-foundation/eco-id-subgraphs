{
  "name": "eco-id-subgraphs",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "network": "node scripts/setupNetwork.js :",
    "build": "graph build",
    "deploy": "graph deploy --product hosted-service ecographs/eco-id-subgraphs",
    "create-local": "graph create --node http://localhost:8020/ ecographs/eco-id-subgraphs",
    "remove-local": "graph remove --node http://localhost:8020/ ecographs/eco-id-subgraphs",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 ecographs/eco-id-subgraphs"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.34.0",
    "@graphprotocol/graph-ts": "^0.28.0",
    "yaml": "^2.1.3"
  }
}
