{
  "name": "claim",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ paged1/claim",
    "create-local": "graph create --node http://localhost:8020/ paged1/claim",
    "remove-local": "graph remove --node http://localhost:8020/ paged1/claim",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 paged1/claim"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.1",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
